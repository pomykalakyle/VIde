{
  "name": "vide-electron-react",
  "version": "1.0.0",
  "description": "Parallel Electron + React app for VIde.",
  "main": "electron-dist/main.js",
  "scripts": {
    "preinstall": "node -e \"const execPath = process.env.npm_execpath || ''; const userAgent = process.env.npm_config_user_agent || ''; const isBun = execPath.includes('bun') || userAgent.startsWith('bun/'); if (!isBun) { console.error('This project uses Bun. Run bun install instead of npm install.'); process.exit(1); }\"",
    "dev": "concurrently -k \"bun run dev:vite\" \"bun run dev:electron:build\" \"bun run dev:electron\"",
    "dev:vite": "vite --strictPort --port 5174",
    "dev:electron:build": "esbuild electron/main.ts electron/preload.ts electron/integration-runner.ts --bundle --platform=node --format=cjs --outdir=electron-dist --external:electron --watch",
    "dev:electron": "bun run electron/dev-runner.ts",
    "build": "bun run build:renderer && bun run build:electron",
    "build:renderer": "vite build",
    "build:electron": "esbuild electron/main.ts electron/preload.ts electron/integration-runner.ts --bundle --platform=node --format=cjs --outdir=electron-dist --external:electron",
    "test": "bun test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "packageManager": "bun@1.3.10",
  "dependencies": {
    "dockview-core": "^5.1.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "@types/node": "^25.3.5",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.0",
    "bun-types": "^1.3.10",
    "concurrently": "^9.2.1",
    "electron": "^40.8.0",
    "esbuild": "^0.27.3",
    "tailwindcss": "^4.2.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
